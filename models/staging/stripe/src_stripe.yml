version: 2

sources:
  - name: stripe
    database: dbt-tutorial
    description: Clone of stripe db
    tables:
      - name: payment
        description: stripe Payment data.
        load_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
